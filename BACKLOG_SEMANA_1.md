# Backlog Semana 1

1) Setup CI básico (lint + unit + integration) con coverage gate.
   - Criterio: pipeline verde con reporte de coverage.
2) Implementar dominio mínimo para generación determinista y scoring `matched`.
   - Criterio: tests unitarios de seed, scoring y constraints pasan.
3) Validación de `map_spec` con shapes dentro de límites.
   - Criterio: test unitario de validación pasa.
4) Endpoints Flask para generar y listar cartas (stubs).
   - Criterio: endpoints responden 200.
5) UI Gradio consume API.
   - Criterio: UI muestra resultado básico.
